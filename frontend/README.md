# Minimal Frontend

This is a minimal React + Vite frontend to interact with the existing backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/rest` to `http://localhost:3000`.
