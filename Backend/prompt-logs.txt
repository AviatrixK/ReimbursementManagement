1. I am building a backend role-based access control reimbursements tool in JavaScript 
   (Node.js >=  20.10.2) using Express.js and Supabase PostgreSQL. 

    I need to implement the database schema and initialization scripts. According to the specification, I must expose two scripts in package.json:
    1. "db:migrate": Sets up the required database structure.
    2. "db:seed-data": Seeds EXACTLY and ONLY the root CFO user:
    - Email: cfo@org.com
    - Password: CFO#ORG@April2026
    - Role: CFO

    System details & requirements:
    - Roles available: EMP, RM, APE, CFO.
    - Every EMP reports to exactly one RM (handle this hierarchical relationship cleanly using foreign keys or a clean assignments junction).
    - Reimbursements have statuses: PENDING, REJECTED, APPROVED.
    - An EMP's reimbursement only turns APPROVED from the EMP's perspective once both their assigned RM and any one APE have explicitly patched it to an approved state. Therefore, we need to track individual manager and APE approval signatures/actions.

    Please write the clean, production-ready SQL migration files along with the package.json scripts configuration to handle db:migrate and db:seed-data. Do not use TypeScript. Include robust edge-case tracking for passwords (hashed using bcrypt).

2.  Now that our database schema and CFO seed are established, let's implement the public
    onboarding routes. 

    Write the routes, controllers, and services for these exact endpoints:
    1. POST /rest/onboardings/register
    - Request Body: { name, email, password }
    - Rules: Default role must be 'EMP'. Only allow emails belonging to the 'org.com' domain.
    2. POST /rest/onboardings/login
    - Request Body: { email, password }
    - Rules: Only allow 'org.com' domain. Authenticate the user and set a secure HttpOnly auth cookie.
    3. POST /rest/onboardings/logout
    - No payload. Clears the auth cookie.

    Ensure the folder layout segregates routes, controllers, and services cleanly. Use pure JavaScript. Handle errors gracefully and return standardized JSON error messages for invalid domains or bad credentials.

3. Let's build the administration endpoints. These endpoints are protected and can ONLY be called by the root CFO user. 

    Implement an authentication/RBAC middleware that reads the HTTP session cookie, validates the session, identifies the user's role, and blocks unauthorized roles with a 403 Forbidden status.

    Next, build the routes, controllers, and services for:
    1. POST /rest/roles/assign
    - Request body: { userId, role }
    - Restricted to: CFO only.
    2. POST /rest/employees/assign
    - Assigns an employee to a reporting manager.
    - Request body: { userId, managerId } (where userId belongs to an EMP and managerId belongs to an RM).
    - Restricted to: CFO only.
    3. DELETE /rest/employees/assign
    - Removes the reporting assignment between an employee and an RM.
    - Request body: { userId, managerId }
    - Restricted to: CFO only.

    Write defensive code checking edge cases (e.g., ensuring you can't assign an employee to a manager who doesn't hold the 'RM' role).
4. We need to implement the directory listing endpoint: GET /rest/employees.

    This endpoint has complex visibility rules based on the requester's role:
    - EMP: Not allowed to access this API (403 Forbidden).
    - RM: Lists ALL the EMPs reporting directly to them.
    - APE: Lists ALL EMPs and RMs in the system.
    - CFO: Lists absolutely everyone.

    Implement the route, controller logic, and the precise database query layers to selectively filter the user records returned based on the requester's extracted cookie identity and role. The response shape must strictly follow:
    {
    "status": "success",
    "data": {
        "users": [ { "userId": "...", "name": "...", "email": "...", "role": "..." } ]
    }
    }

5. Let's build the core engine of the system: the reimbursement lifecycle. 

    Implement the following endpoints with strict domain validation rules:
    1. POST /rest/reimbursements
    - Action: Raises a new claim.
    - Restricted to: EMP only.
    - Request body: { title, description, amount }
    - Initial behavior: Status sets to PENDING.

    2. PATCH /rest/reimbursements
    - Action: Approves or rejects an existing claim.
    - Request body: { userId, status }  *(Note: userId corresponds to the employee who raised the reimbursement, status is either APPROVED or REJECTED)*
    - Restrictions: EMP cannot edit a reimbursement once raised. RM, APE, and CFO can approve or reject.
    - **CRITICAL APPROVAL RULE**: A reimbursement status turns APPROVED from the EMP's perspective ONLY once both their designated RM AND at least one APE have explicitly approved it. If only one has approved it, or if it's waiting on another layer, its composite state reflects appropriately based on who is viewing it. 

    Write this logic cleanly in the service layer using a ledger approach or multi-signature flag tracking columns in our PostgreSQL database.

6.  Let's complete the final components of the API specification: fetching reimbursement queues.

    Implement the following two lookup endpoints:
    1. GET /rest/reimbursements
    - Visibility rules:
        * EMP sees only their own claims.
        * RM sees claims that are PENDING from their direct subordinate EMPs.
        * APE sees claims PENDING at the APE level but already APPROVED by the employee's RM.
        * CFO sees claims already APPROVED by the APEs.

    2. GET /rest/reimbursements/<user-id>
    - Action: Lists all the claims for the given user-id parameter.
    - Rule: Only accessible if that targeted user is an EMP AND is a direct subordinate of the requesting user.

    Match the exact output JSON payload response formats specified in the documentation. Run a mental edge-case inspection for security flaws (like ID-harvesting or horizontal privilege escalation) to guarantee our system is fully watertight.

